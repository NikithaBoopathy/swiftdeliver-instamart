apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: instamart-ingress
spec:
  rules:
  - http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: instamart-backend-svc
            port:
              number: 80
